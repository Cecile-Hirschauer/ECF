"use client"
import React from 'react';
import {Flex} from "@chakra-ui/react";

const StakeToken = () => {
    return (
        <Flex >
            Stake
        </Flex>
    );
};

export default StakeToken;
